/* Copy values from your hosting provider here. Never put Google client secrets in this file. */
window.MOOD_API_CONFIG={
  // Leave empty to use the same server. Example external server: "https://api.example.com"
  API_BASE_URL:"",
  // Create this in Google Cloud Console; it is safe to expose a browser client ID.
  GOOGLE_CLIENT_ID:"",
  GOOGLE_REDIRECT_URI:"",
  ENABLE_GOOGLE_LOGIN:false
};
