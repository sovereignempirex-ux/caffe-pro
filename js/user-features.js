(()=>{const key='mood-favorites',favorites=JSON.parse(localStorage.getItem(key)||'[]');const save=()=>localStorage.setItem(key,JSON.stringify(favorites));document.querySelectorAll('.menu-grid .card').forEach(card=>{const name=card.dataset.name,heart=document.createElement('button');heart.className='favorite-toggle';heart.type='button';heart.setAttribute('aria-label','حفظ في المفضلة');heart.textContent=favorites.includes(name)?'♥':'♡';heart.onclick=e=>{e.stopPropagation();const i=favorites.indexOf(name);i<0?favorites.push(name):favorites.splice(i,1);heart.textContent=favorites.includes(name)?'♥':'♡';save()};card.appendChild(heart)});const list=document.getElementById('favorite-list');if(list){list.innerHTML=favorites.length?favorites.map(x=>`<span class="favorite-chip">♥ ${x}</span>`).join(''):'<p class="empty-cart-msg">لا توجد مفضلات بعد. افتح المنيو واضغط ♥.</p>'}const notifications=document.getElementById('notifications-switch');if(notifications){notifications.checked=localStorage.getItem('mood-notifications')!=='off';notifications.onchange=()=>localStorage.setItem('mood-notifications',notifications.checked?'on':'off')}})();
