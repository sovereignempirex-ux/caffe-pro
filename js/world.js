/* Lightweight canvas world: stylized game-like cafe ambience without a heavy 3D dependency. */
(()=>{const c=document.getElementById('cafe-world');if(!c)return;const ctx=c.getContext('2d'),reduce=matchMedia('(prefers-reduced-motion: reduce)').matches;let w,h,particles=[],mx=.5,my=.5;
function resize(){w=c.width=innerWidth*devicePixelRatio;h=c.height=innerHeight*devicePixelRatio;c.style.width=innerWidth+'px';c.style.height=innerHeight+'px';ctx.setTransform(devicePixelRatio,0,0,devicePixelRatio,0,0);particles=Array.from({length:innerWidth<700?34:72},()=>({x:Math.random()*innerWidth,y:Math.random()*innerHeight,r:Math.random()*2+.4,s:Math.random()*.35+.08,t:Math.random()*6.3}))}
addEventListener('resize',resize);addEventListener('pointermove',e=>{mx=e.clientX/innerWidth;my=e.clientY/innerHeight});resize();
function draw(time){ctx.clearRect(0,0,innerWidth,innerHeight);const gx=innerWidth*(.42+(mx-.5)*.09),gy=innerHeight*(.36+(my-.5)*.07);let g=ctx.createRadialGradient(gx,gy,5,gx,gy,Math.max(innerWidth,innerHeight)*.55);g.addColorStop(0,'rgba(255,194,116,.14)');g.addColorStop(1,'rgba(0,0,0,0)');ctx.fillStyle=g;ctx.fillRect(0,0,innerWidth,innerHeight);ctx.save();ctx.translate((mx-.5)*16,(my-.5)*10);particles.forEach(p=>{const y=(p.y-time*p.s*.035)% (innerHeight+20);ctx.beginPath();ctx.arc(p.x,y<0?y+innerHeight:y,p.r,0,Math.PI*2);ctx.fillStyle=`rgba(255,${185+Math.floor(45*Math.sin(time*.001+p.t))},123,${.22+p.r*.08})`;ctx.fill()});ctx.restore();
const floor=ctx.createLinearGradient(0,innerHeight*.68,0,innerHeight);floor.addColorStop(0,'rgba(16,9,7,0)');floor.addColorStop(1,'rgba(0,0,0,.6)');ctx.fillStyle=floor;ctx.fillRect(0,innerHeight*.68,innerWidth,innerHeight*.32);if(!reduce)requestAnimationFrame(draw)}if(reduce)draw(0);else requestAnimationFrame(draw)})();
